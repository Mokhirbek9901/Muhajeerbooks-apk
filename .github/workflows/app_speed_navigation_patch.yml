name: Apply app speed navigation patch

on:
  push:
    paths:
      - 'app_speed_navigation_trigger.txt'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply speed and navigation patch
        run: python3 tools/app_speed_navigation_patch.py
      - name: Apply stronger scroll jank fix
        run: python3 tools/scroll_jank_fix.py
      - name: Commit source update
        run: |
          git config user.name "Mokhirbek9901"
          git config user.email "ismoilovmokhirbek@gmail.com"
          git add lib/app_state.dart lib/store_ui.dart lib/catalog_resume.dart lib/admin_ui.dart lib/fast_store_shell.dart pubspec.yaml
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Eliminate storefront scroll jank"
          git push origin main

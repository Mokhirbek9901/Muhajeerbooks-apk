name: Fix admin refresh and customer notices

on:
  push:
    branches: [main]
    paths:
      - 'tools/fix_admin_refresh_and_customer_notices.py'
      - '.github/workflows/fix_admin_refresh_and_customer_notices.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Apply patch
        run: python3 tools/fix_admin_refresh_and_customer_notices.py
      - name: Format
        run: dart format lib/admin_ui.dart lib/app_state.dart lib/store_ui.dart
      - name: Get packages
        run: flutter pub get
      - name: Analyze
        run: flutter analyze --no-fatal-warnings --no-fatal-infos
      - name: Test
        run: flutter test
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/admin_ui.dart lib/app_state.dart lib/store_ui.dart
          if git diff --cached --quiet; then
            echo 'No changes'
          else
            git commit -m 'Fix admin background refresh and add customer order notices'
            git push origin main
          fi
